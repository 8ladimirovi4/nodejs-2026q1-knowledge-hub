PORT=4000
ADMINER_PORT=8080

POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=knowledge_hub
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Local Nest + Prisma CLI on host → Postgres in Docker (published port 5432).
# The `app` service in docker-compose.yml overrides DATABASE_URL to use host `db`.
DATABASE_URL="postgresql://admin:admin@localhost:5432/knowledge_hub?schema=public"

CRYPT_SALT=10
JWT_SECRET_KEY=secret123123
JWT_SECRET_REFRESH_KEY=secret123123
TOKEN_EXPIRE_TIME=15m
TOKEN_REFRESH_EXPIRE_TIME=7d
